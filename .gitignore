# Dependencies
node_modules/
.pnp/
.pnp.*

# Next.js build output
/.next/
/out/
/build/
next-env.d.ts
*.tsbuildinfo

# OpenNext + Cloudflare (build artifacts / generated)
/.open-next/
/.wrangler/

# Environment & secrets
.env
.env.local
.env.*.local
.dev.vars
.dev.vars.*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/

# Content library — full-res scraped originals are large (~1.8GB): keep LOCAL, don't commit.
# The committed, portable record is the catalog (catalog.csv / CATALOG.md) + Wix CDN URLs,
# plus optimized previews under content-library/preview/ (committed).
/content-library/images/
/content-library/_pages_html/
